{
	"name": "@spectracker/db",
	"type": "module",
	"exports": {
		".": {
			"default": "./src/index.ts"
		},
		"./*": {
			"default": "./src/*.ts"
		}
	},
	"scripts": {
		"db:local": "echo 'Run: pnpm run dev:server'",
		"db:push": "drizzle-kit generate && pnpm -F server exec wrangler d1 migrations apply specdev --local",
		"db:generate": "drizzle-kit generate",
		"db:studio": "echo 'To use Drizzle Studio, use: drizzle-kit studio --dialect sqlite --url ../../apps/server/.wrangler/state/v3/d1/miniflare-D1DatabaseObject/*.sqlite'",
		"db:migrate": "drizzle-kit generate && pnpm -F server exec wrangler d1 migrations apply specdev --local",
		"db:migrate:remote": "drizzle-kit generate && pnpm -F server exec wrangler d1 migrations apply specdev --remote"
	},
	"dependencies": {
		"@libsql/client": "catalog:",
		"dotenv": "catalog:",
		"drizzle-orm": "^0.45.1",
		"libsql": "catalog:",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@spectracker/config": "workspace:*",
		"drizzle-kit": "^0.31.9",
		"typescript": "catalog:"
	}
}
